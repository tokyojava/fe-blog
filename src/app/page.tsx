import { Alert, AlertDescription, AlertTitle } from "@/components/ui/alert";
import Image from "next/image";

export default function Home() {
  return (
    <div className="">
      <Alert>
        <AlertTitle>AA</AlertTitle>
        <AlertDescription>
          Description
        </AlertDescription>
      </Alert>
    </div>
  );
}
